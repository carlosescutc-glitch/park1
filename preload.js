// Preload script for Precision Park Electron App
// Provides bridge between node and renderer process if needed in the future

window.addEventListener('DOMContentLoaded', () => {
  console.log('Precision Park Preload Loaded Successfully.');
});
